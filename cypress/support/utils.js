
const randomNo = Math.floor(1000000 * Math.random() + 1).toString();
export const randomUsername = randomNo + '_username';
export const randomEmail = randomNo + '@dispostable.com';
export const randomName = randomNo + '_name';